
<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">

    <div class="container">
   
          
      <nav>
<br><br><br>
        <ul class="nav pull-right">
           <% if signed_in? %>
          
          <% end %>
          <li><%= link_to "Home", root_path %></li>
          <li><%= link_to "Fresh Produce Listings", products_path %></li>
          
          <% if signed_in? %>
        
            
            <% if current_user.unread_messages? %>
            <li><%= link_to "Inbox (Unread Message)", user_messages_path(current_user) %></li>
          <% else %>

          <li><%= link_to "Inbox", user_messages_path(current_user)  %></li>
          <% end %>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                My Account <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "My Profile", current_user %></li>
                
                <li><%= link_to "Account Settings", edit_user_registration_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", destroy_user_session_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Sign in",   new_user_session_path %></li>
          <% end %>
          
        </ul>
      </nav>
    </div>

  </div>
</header><br>